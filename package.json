{
  "name": "kusoogle",
  "version": "1.0.0",
  "private": true,
  "description": "クソアプリ検索エンジン",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "dev:frontend": "bun run --filter './apps/frontend' dev",
    "dev:search": "cd apps/workers/search && bun run dev",
    "dev:data-collection": "cd apps/workers/data-collection && bun run dev",
    "build": "bun run --filter './apps/*' build",
    "build:shared": "bun run --filter './packages/shared' build",
    "test": "bun test",
    "typecheck": "bun run --filter './apps/*' typecheck && bun run --filter './packages/*' typecheck",
    "initialize-data": "./scripts/initialize-data.sh",
    "deploy:search": "cd apps/workers/search && bun run deploy",
    "deploy:data-collection": "cd apps/workers/data-collection && bun run deploy",
    "deploy:workers": "./scripts/deploy-workers.sh",
    "deploy:frontend": "cd apps/frontend && NODE_ENV=production bun run build && bun run deploy:cf"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}